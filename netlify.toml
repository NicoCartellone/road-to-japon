# example netlify.toml
[build]
  command = "npm run build"
  publish = "dist"

  [build.environment]
    # Netlify Secrets Scanning: evita falso positivo.
    # En este repo el valor de VITE_FIREBASE_PROJECT_ID coincide con textos del proyecto
    # (p.ej. package.json), y Netlify falla el build al detectarlo como "secreto".
    SECRETS_SCAN_OMIT_KEYS = "VITE_FIREBASE_PROJECT_ID"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

  ## (optional) Settings for Netlify Dev
  ## https://github.com/netlify/cli/blob/main/docs/netlify-dev.md#project-detection
  #[dev]
  #  command = "yarn start" # Command to start your dev server
  #  port = 3000 # Port that the dev server will be listening on
  #  publish = "dist" # Folder with the static content for _redirect file

  ## more info on configuring this file: https://ntl.fyi/file-based-build-config
